import { NAME } from './constants';

const prefix = `${NAME}/`;
export const RECIEVE_RECOGNITION_TASKS = `${prefix}RECIEVE_RECOGNITION_TASKS`;
export const ADJUST_RECOGNITION_TASKS = `${prefix}ADJUST_RECOGNITION_TASKS`;


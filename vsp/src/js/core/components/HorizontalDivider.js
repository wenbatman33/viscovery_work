import React from 'react';
import CSSModules from 'react-css-modules';
import styles from '../styles/layout.scss';

const HorizontalDivider = () => <div styleName="divider" />;

export default CSSModules(HorizontalDivider, styles);

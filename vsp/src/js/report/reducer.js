/**
 * Created by amdis on 2017/3/22.
 */
const initialState = {
  fake: null,
};

export default function report(state = initialState, action) {
  switch (action) {
    default:
      return state;
  }
}
